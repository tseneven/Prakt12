<Page x:Class="Prakt12.Pages.GroupInfo"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Prakt12.Pages"
      mc:Ignorable="d" 
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GroupInfo">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Button Click="Button_Click">Назад</Button>
            <TextBlock Text="{Binding interestGroup.Title}" FontSize="30" FontWeight="Bold"/>
            <TextBlock Text="{Binding interestGroup.Description}" FontSize="20"/>
        </StackPanel>
        <ListView Grid.Column="1" ItemsSource="{Binding service.userInterestGroups}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBlock>Логин</TextBlock>
                            <TextBlock Text="{Binding User.Login}"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBlock>Имя</TextBlock>
                            <TextBlock Text="{Binding User.Name}"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBlock>Почта</TextBlock>
                            <TextBlock Text="{Binding User.Email}"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBlock>Запись создана</TextBlock>
                            <TextBlock Text="{Binding User.CreatedAt}"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBlock>Роль</TextBlock>
                            <TextBlock Text="{Binding User.Role.Title}"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBlock>Статус</TextBlock>
                            <TextBlock Text="{Binding IsModeratorText}"></TextBlock>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
